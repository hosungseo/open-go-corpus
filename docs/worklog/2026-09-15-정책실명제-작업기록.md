# 작업기록 — 정책실명제 진단·비교분석·대조기·리디자인 (2026-09-15 ~ 16)

## 배경
- 실장 지시(9월 2주): 정보공개포털·정책실명제에 AI를 써 보라. 이후 회의 녹취로 실제 질문이 드러남 — **정책실명제·사전정보공표·원문공개의 공개 깊이·주기·대상을 팩트로 비교해 활성화/축소/폐지를 판단하고, 정보공개법 §7 아래 정책실명제 조항 신설(공공기관 확장) 여부를 국무회의 자료로**. 대통령 관심사항.
- 정보공개과 담당자 증언: 5월 지침·연말 자체평가로 끝나는 가벼운 업무, 국민신청 작년 36건, 지자체는 홈페이지 링크뿐이고 현행화 없음, 중앙은 온나라 관리카드 연결로 결재문서 목록이 뜸.

## 한 일 (시간순)
1. **정책실명제 코너 전수 수집** — 포털 `polRnInsttList.ajax` → `polRnBList.do`(페이지 내장 JSON) → `polRnInsttDetail.do`. 세션 없이 받아짐. 중앙 2018~2026 6,847행(ID 4,702개), 2025~26 상세 784행. 도구 `tools/collect-realname.mjs`, `collect-realname-detail.mjs`.
2. **지자체 링크 261개 생존 점검** — 404·응답없음 3, 메인·업무추진비·로그인으로 오도 14.
3. **AI 구상안** — 진단 6항(참여 42→7기관, 조회 중앙값 10→0, 지자체 데이터 0, 자유입력·엔티티 56%, ID 비일관, 상류 결재문서 260건) + 과제 5(가 사업↔결재문서 자동연결 / 나 지자체 심의결과 HWP 수확 / 다 정제·계보 / 라 추진실적 초안 / 마 질의응답·국민신청) + 로드맵.
4. **3제도 비교분석** — 사전정보 통계·상세 표본(주기 매년 41·수시 40 /100)·링크 120 표본, 원문공개율(2025 중앙 49%·지자체 59%·교육청 84%·공공 75%), 정보공개법 §7·§8의2·시행령 §4·§5의2·행정업무규정 §63 조문. 같은 사업 3채널 대조 6건. 결론: 단위가 다름(사업/항목/문서), 정책실명제만 사업 단위 책임 명부인데 예산 필드 없음·연 1회·조회 0.
5. **대조기 구현** — 설계 문서 5종(PRD·기능명세·유저플로우·와이어프레임·검수) → 배치 대조 수집 `probe-realname-batch.mjs`(724 ID, 검색어=사업명 최장 명사구; 사전정보 0건 298·원문 0건 373·둘 다 0건 209) → `build-explorer.mjs`(실명 마스킹 3단계+각주·값 무결성 검사) → 단일 HTML.
6. **정정** — "같은 사업이 매년 새 ID"는 틀림. 반복 사업 1,380개 중 ID 유지 621·새 ID 759 → "연결 비일관". 복합키 `gclfCd|year` 채택.
7. **공개 배포** — GitHub public `hosungseo/open-go-corpus`, Pages = main/docs. 사용자가 `!` 명령으로 직접 생성·푸시(자동 모드가 `gh repo create`·`gh api pages` 차단).
8. **jiro 참고** — Stats & Metrics 03 Halo(줄무늬 비율 막대), Pricing 05 Pixa(●◐○ 기능 매트릭스) 패턴만 이식.
9. **가독성 리디자인**(9/16) — 실장 피드백 "보기 힘들다". /office-hours(3차 적대적 검토, 32건 반영, 8/10) → /plan-ceo-review HOLD SCOPE(결정 D6~D8·1A·4A) → 구현. 사용자 정정: **보고서(개조식) 형식이 아니라 웹 문서 형식 유지, 선택지(폐지·유지·편입) 절 삭제**.
   - 공통 문법: 상단 고정 바(기준일·범례) → 요약(결론 1문장·파란 강조 1곳·큰 숫자 3) → 핵심 절 ≤3 → 별첨(상세 표·매트릭스·각주, 접지 않음).
   - `docs/src/shared.css` + `docs/src/*.html` 소스, 빌드가 CSS 인라인해 `docs/*.html` 생성. CSS 부재·로컬 link 잔존은 빌드 실패.
   - 대조기: 스트립 3지표, 기본 예시 '행정안전부·풍수해보험사업 운영'(기관+사업명 상수→ID, 부재 시 첫 항목 폴백), Q&A는 비교분석에만, 3열 카드는 대조기 별첨에만.
   - 검수: 1366·1024·860px 스크린샷 12장, 가로 스크롤 0, 요약 블록 첫 화면 내, 정적 페이지 실명 0건.

## 산출물
- 공개: https://hosungseo.github.io/open-go-corpus/ (개요) · `/compare.html` · `/ai-plan.html` · `/explorer/`
- 저장소: `~/open-go-corpus` (= `/Volumes/서호성SSD(2TB)/open-go-corpus`). 커밋 eb48077 → 97980a4 → 6d9e159 → 3510675 → 651292b → c64dd8b → f9d1234 → 860fa15 → 2645d2c → 0f228f6 → c5b0172
- 발견 정본 `FINDINGS.md` §11~§13. 설계 `docs/designs/readable-redesign.md`(APPROVED, CEO 리뷰 보고서 포함), `docs/explorer/01~05-*.md`
- 아티팩트: 대조기 ef61d49e… · 비교분석 91938fee… · 구상안 0200c077…
- 수집 데이터(`realname/`, `raw/`, 본문 PDF, 실명 포함 로컬 빌드)는 gitignore, 로컬 전용.

## 미확인·다음
- 기관당 권고 건수 지침 원문, 원문공개 "국장급 이상" 범위 조문, 공공기관 ALIO 항목 대조, 사전정보 검색 ajax 404 원인(UI 경유는 정상).
- 실장 보고 후 반응 대기. 편입 방향이면 AI 구상안 과제 가·나·라가 실행 수단.
- 푸시 대기 커밋 3개(2645d2c·0f228f6·c5b0172): `git push` 후 Pages 반영 확인, 실물에 `/design-review`.

## 함정 메모
- 이 환경에서 `pkill -f`가 안 먹혀 배치 3개가 동시에 돌았음 → `pgrep` 후 `kill -9 PID`.
- 사전정보 검색 ajax는 날짜 `YYYY-MM-DD`+`sort=s` 필요.
- 자동 모드 분류기가 `gh repo create`·`gh api …/pages`·`git push`를 막음 → 사용자가 `!` 명령으로 실행.
- `docs/*.html`을 직접 고치면 빌드가 덮어씀. `docs/src/`를 고칠 것.

## 9/16 밤 — 사이트 정비 (Aside)
- 페이지 순서 개요 → 세 제도 비교 → 사업 대조기 → **정책실명제 2.0**(구 AI 구상안, `policy2.html`; `ai-plan.html`은 리다이렉트).
- 세 제도 비교: 이중 레이어. 1층 아홉 기준 매트릭스(행 클릭 → 2층), 2층 항목 15개 탭(근거와 대상/무엇을 공개하나/얼마나 언제/이용과 품질) + 3열 상세 카드. 삭제: 148자·20자·28,659자 박스, "물으신 여섯 가지", "같은 사업 3채널" 절·별첨 사례표. 근거·방법 [3]에 원문공개 "국장급·부단체장 등 이상" 문구 출처(포털 안내문, 법령 부재) 추가.
- 사업 대조기: 41/52/29% 비율 스트립 제거, 헤더 바로 아래 예시 사업.
- 정책실명제 2.0: 행안부 「주민자치회 법제화 및 활성화」 4년 계보(‘22 연구 → ‘23 조례안 → ‘24 현황조사 → ‘25 법제화, 개명·소관 이관 마커) + 후속 조치 가지 + 연계 문서 목록 + 우측 원문 뷰어(비공개/공개-원문대상아님/원문 3상태). 원문은 부산시 「2026년 구·군 주민자치회 운영평가 계획」(포털 4단계 다운로드로 실제 수신, 7쪽 이미지, 결재란 성명 픽셀화). 실측: '25 관련 정보목록 184건은 §8 정보목록이며 표본 14건 중 원문 열람 0(비공개 12, 과장 전결 2).
- 개요: 내비·카드 순서/문안, 미리보기 3장 재생성.
- 검수: 로컬 서버(8931)에서 4페이지 렌더·클릭 동작·콘솔 오류 0. 정적 페이지 실명 0(직급·○○○만).
- 푸시는 사용자가 직접.
- jiro 참고 2차(Luminio 템플릿): ① 정책실명제 2.0 "세 가지 장치" 카드에 mono 태그 + 미니 UI SVG(정책 번호 허브 / 결재선 위 번호 선택 / 근거문서 4칸 중 미등록·비공개) ② 세 제도 비교 2층을 알약 탭 → 왼쪽 고정 항목 목록(01~15, 4묶음) + 오른쪽 상세 카드로 ③ 핵심 절 h2 위 mono 눈썹(1층 · 아홉 기준 / 2층 · 항목별 실측 / 장치 · 셋 / 화면 · 행정안전부 주민자치회). 다크·글로우·캐러셀은 불채택.
- 개요 페이지 교체(jiro CryptoCalc 패턴): 크림 배경·64px 세리프 헤드라인 + 우측 **동작하는 미니 대조기**(2025년 대표 사업 6건: 풍수해보험·고용서비스·성별영향평가·금융소비자·마이데이터·토큰증권, 세 채널 건수·첫 항목·판정 문장, 대조기 딥링크) → 실측치 티커(10개, 마퀴) → 아홉 기준 요약 표(compare 2층 딥링크) → 세 페이지 카드 → 다크 CTA(정책실명제 2.0 미니 요약). Lumora 버전은 git 이력(19cb498)에.
- **KRDS 적용**(github.com/KRDS-uiux, npm krds-uiux 1.1.0 `resources/css/token/krds_tokens.css` 실측): `shared.css` 토큰 전면 교체 — 서체 Pretendard GOV(CDN dynamic subset), 본문 17px/1.5, 제목 heading-xlarge 40·large 32·small 19, 색 gray-90 텍스트·gray-70 보조·gray-20 테두리·primary-50 #256ef4·text-primary #0b50d0, 세 제도 색을 point-50/warning-50/success-60으로, 반경 4/8/10. 마스트헤드(개인 시안 고지, 공식 누리집 아님 명시)·다크 푸터 식별자 4페이지 공통. 개요 크림 팔레트 → 흰 바탕+gray-5, 버튼 KRDS primary/secondary(높이 56·반경 8), 다크 CTA secondary-90. 세리프·모노 자리는 같은 서체로(굵기·tabular-nums만). 미리보기 3장 재생성.
- 비유 블록 "병원으로 치면: 창구 셋 → 차트 한 장" 추가 — 개요(티커 아래)와 정책실명제 2.0(장치 카드 위). 선 그림 SVG 2장(안내문/처방전 더미/담당의 명단 ↔ 담당의·처방·검사·비용이 붙은 차트, 정책 번호 = 차트 번호).
- **접근성·전달 전 점검**(ui-ux-pro-max 스킬의 pre-delivery 체크리스트 기준으로 자가 점검, 스킬 설치는 안 함): 자동 점검 결과 걸린 항목을 수정 — ① KRDS 스킵 내비(`본문 바로가기`) 4페이지 추가 ② `<div class="wrap">` → `<main id="content">` 랜드마크 ③ 포커스 표시(primary 3px outline) 전역 ④ `prefers-reduced-motion` 전역(티커·전환 정지) ⑤ 클릭만 되던 요소를 키보드 조작 가능하게 — 비교 매트릭스 행 9개, 2.0 타임라인·문서·노드 토글 27개에 role/tabindex/Enter·Space, aria-expanded·aria-pressed·aria-current, 상세 영역 aria-live ⑥ 상태 아이콘 이모지(✓ ◔ ✕ 🔒) 19곳을 인라인 SVG로 교체 ⑦ 유령 노드의 잘못된 포인터 커서 제거 ⑧ 560px 이하 히어로·CTA·위젯 규칙 추가. 미검증: 실제 375/768px 렌더(환경상 뷰포트 변경 불가, CSS 분기만 확인).
- 설명문 문체·줄바꿈 정비: 4페이지 히어로 리드와 각 절 도입 문단, 비유 그림 캡션을 **습니다체**로 바꾸고 의미 단위로 줄바꿈(세 제도 정의는 제도마다 한 줄). 대조기에는 없던 세 제도 설명문을 리드에 추가("세 채널은 같은 사업을 서로 다른 단위로 담기 때문에, 이름이 같아도 나오는 것이 다릅니다"). 표·각주·판정 문구·데이터 캡션은 간결체 유지.
- 전산 용어 → 행정 용어 교체(세 제도 비교 중심, 개요·대조기의 같은 문구도 함께): 레코드→등록 건, 필드→기재 항목·기재란·적는 칸, 정형→규격 통일, 자유입력→서술식 기재, 데이터 품질→기재 방식, 적재→등재, ID→관리번호, URL→누리집 주소, 타임아웃→응답 없음, 스냅샷→기준, (n=784)→(784건 기준). 판정 문구도 "정책실명제는 코드가 아니라 글자"→"분류 코드 없이 서술로만", "예산·집행은 셋 다 정형 필드 없음"→"셋 다 적는 칸이 없다".
- 비유·피동 표현 정리: "가장 얕고 가장 읽히지 않습니다"→"내용이 가장 적고, 조회도 가장 적습니다", "도달하지 않는다"→"조회가 거의 없음", "기관 공개율에 갇힌다"→"기관 공개율에 좌우", "셋을 잇는 열쇠"→"셋을 잇는 고리", "경과의 실체는 원문"→"추진 경과는 원문에", "살아 있는 관련 문서"→"갱신되는 유일한 관련 문서", "한 줄로 덮인다"→"한 줄로 끝난다", "관문 노릇을 한다"→"문서로 바로 갈 수 있습니다", "실체가 없다"→"내용을 찾을 수 없습니다". 판정 라벨 15개를 명사형으로 통일(…없음/…에/…좌우), 개요 위젯 판정문 4종은 습니다체 전환, 2.0 장치 카드 제목 3개도 습니다체로.
- 비유 블록 가독성 정비: SVG가 520 단위인데 484px로 축소돼 글자가 12px 아래로 눌리던 문제 → viewBox 520×260 → **560×310**으로 다시 그리고(창구 헤더를 "1번 창구"/"사전정보공표" 두 줄로 분리, 본문 13.5px·설명 12px로 확대, 창구 폭 150→176, 차트 4칸 확대), 블록 폭을 본문(1132px)보다 넓은 `min(1360px, 100vw-48px)`로 좌우 확장. 1440~990px 전 구간 가로 스크롤 없음 확인, 980px 이하는 기존대로 세로 배치.

## 9/16 — 법령 연혁 탭 신설 (법제처 API + aside)
- **탭 5개 체제**: 개요 → 세 제도 비교 → **법령 연혁** → 사업 대조기 → 정책실명제 2.0. 다섯 페이지 내비 모두 교체.
- **수집**: 국가법령정보센터 DRF에서 `LID`(법령ID)로 시행본 전수. 정보공개법(001357) 18 · 시행령(002255) 23 · 사무관리규정 계열(003728) 50 = **91개, 조문 7,713개**. 도구 `tools/collect-law-history.mjs` → `tools/build-law-timeline.mjs`(변화 지점+제·개정이유) → `tools/build-history-data.mjs`(페이지용 압축). 빌드가 `/*__LAWDATA__*/` 자리에 주입.
- **핵심 발견**
  - 정책실명제는 **1998-07-01 사무관리규정 §34의2**로 신설. 개정이유 원문: "기안문에는 결재선상에 있는 관련자는 표시되나, 그외 정책의 입안 및 집행에 관여한 다른 관련자들의 실명 및 의견은 표시되지 아니하므로" — **결재선 밖 참여자를 잡으려던 제도**인데 지금 포털 데이터는 결재선 2~3명을 옮겨 적고 있다. 사이트 전체 논지와 맞물림.
  - 2011-12-21 전부개정에서 §34의2 → **§63 「정책의 실명 관리」**. "정책실명제"라는 말이 조문에서 사라짐(삭제 아님, 개명). 처음엔 2년 공백으로 오독했다가 §60~66 제목 확인으로 정정.
  - 2014-02-18 **§63의2~63의5 신설**(책임관·중점관리 대상·평가·세부규정). 개정이유에 **정부 3.0** 명시. 지금 포털의 "중점관리 대상사업"이 여기서 나옴. 원문공개 법률 조문 시행(2014-03-01)과 **2주 차이 쌍둥이**.
  - 2018-11-27 **국민신청** 조문 신설("국민이 중점관리가 필요하다고 신청하는 사업"). 조문은 있는데 포털에 창구가 없다.
  - 원문공개 법률 §8의2는 **2014년 이후 12년간 개정 0회**. 반면 시행령 §5의2가 **2013-11-13**로 모법(2014-03-01)보다 3개월 반 먼저 시행. 그 시행령 개정이유에 단계별 시행일(시도 '14.3.1 / 교육행정기관·시군구 '15.3.1 / 공기업·준정부 '16.3.1)이 있어 **교육청 6,022만 건**의 배경이 됨.
  - 사전정보공표 §7은 제정법(1998)에 없음 — 2004-07-30 전부개정 신설. 2020-12-22 제목이 「행정정보의 공표 등」 → **「정보의 사전적 공개 등」**.
  - 근거 법령 제명 4회 변천: 사무관리규정 → 행정업무의 효율적 운영에 관한 규정('11) → 행정 효율과 협업 촉진에 관한 규정('16) → 행정업무의 운영 및 혁신에 관한 규정('23).
- **페이지 구성**: 요약(1998/2004/2014 큰 숫자 3) → 세 줄 가로 연표(점 12개, 클릭 시 조문 원문+개정이유) → 신설 이유 카드 3 → 차이 표 4행 → 별첨(현행 조문·시행령 먼저 시행·시행본 91 전수·근거 6).
- **개요 보강**(사용자 요청): 2.0 개념을 앞세우는 대신 **「세 제도 · 무엇을 공개하나」 절 신설** — 안내문/처방전 원본/담당의 명단 3카드에 근거·단위·주기·대상·실측 한 줄. 병원 비유 절 바로 위에 배치해 비유로 이어짐.
- **정책실명제 2.0 계보 디자인 정리**(사용자 지적): 황금색 마름모 → 연도 원과 같은 결의 속 빈 고리 · 연도 원의 흰 halo(`box-shadow 0 0 0 4px`)가 가로 이음매를 지워 선이 끊겨 보이던 것 제거 · 파란 세로선을 **아래로 흐르는 점선**(radial-gradient + 2.6s, reduced-motion 시 정지) · 연도 원을 선 중심에 정렬(`left:7.5px`, box-sizing이 border-box라 지름 34px) · 이름 변경 카드의 2색 그라데이션 → 왼쪽 강조선.
- **검증**: aside CLI로 5라운드. 콘솔 오류 0, 가로 스크롤 0(1440·1024·430·390·360px), 연표 점 12개 전부 제 카드 개방, 히트박스 24×42px(WCAG 24 충족), 인접 점 겹침 0. 고친 것: 연도 라벨 겹침 → 자리 유지 숨김 + 정확한 시행일 칩, 점끼리 클릭 뺏김 → 버튼 폭 24px + 최소 간격 24px 밀어내기, 개요 아홉 기준 표가 430px에서 잘리고 스크롤 불가(`.ov{overflow:hidden}`) → `overflow-x:auto`.
- **남은 것**: 430px에서 병원 비유 SVG 글자가 실효 7px로 작음(스택 전환 필요). 정책실명제 운영 지침(행안부 예규)·지자체 조례는 미대조. `lawhist/raw`와 `articles.json`은 gitignore(재수집 가능).

## 함정 메모 (추가)
- DRF `lawSearch.do?target=eflaw`는 **`query=`(법령명)로 받으면 개명 전 판을 놓친다.** `LID=<법령ID>`로 받아야 사무관리규정 50판이 한 줄로 이어진다. `target=law&LID=`는 빈 결과.
- `type=JSON`은 `{}`를 주는 경우가 있다. **XML로 받을 것.**
- 조문 본문 비교 시 **`항내용`만 모으면 `호내용`을 빠뜨려** 실제로 안 바뀐 개정이 "본문 변경"으로 잡힌다. 항·호·목을 다 이어 붙일 것.
- 제·개정이유는 `target=law&MST=<법령일련번호>`로 나온다. 시행본 MST를 그대로 써도 된다. **타법개정 이유는 남의 법 이야기**라 제도 서술에 쓰면 안 된다.
- `new URL("..", import.meta.url).pathname`은 경로를 퍼센트 인코딩해 준다(`/Volumes/%EC%84%9C...`). `fileURLToPath`를 쓸 것 — 한글 볼륨명에서 EACCES로 터진다.

## 9/16 — 정책실명제 2.0 화면 디자인 정비 (8항목)
진단: 이 화면만 KRDS 위에 **두 번째 팔레트**를 얹고 있었다. 하드코딩 색 11종 18회, 동시 강조색 5종(빨강 2·황토 2·청록/초록 2가 거의 같은 색으로 중복), 1px 테두리 컴포넌트 16종에 면 위계 없음, border-radius는 8곳뿐(나머지 페이지는 8px), 글자 크기 13종에 13px 미만이 22회.

- **① 색 일원화** — 하드코딩 11종을 전부 KRDS 토큰으로. `#B8312F→danger`, `#8A6D1F→warning`, `#2E6F73→success`, `#163C7A→primary-70`, `#B9CBE8→primary-20`, `#E3A9A8→danger-10`, `#A9CFCF→success-10`, `#E4D7B0→warning-10`. 검증: 스타일 블록 내 하드코딩 헥스 0.
- **② 강조색 5 → 3+1** — 상태는 셋뿐(있음 success / 부분 warning / 없음 danger), 파랑은 상태가 아니라 구조색(선·선택·연결)으로 역할 고정. 짙은 남색은 파랑에 흡수.
- **③ 면 3단** — 패널(테두리+8px) / 카드(옅은 테두리+4px) / 인라인(바탕만). `.state`는 테두리 대신 왼쪽 3px 강조선.
- **④ 모서리** — 나머지 네 페이지와 같은 `--r-m` 8px·`--r-s` 4px.
- **⑤ 글자 6단** — 11·12·13·14·17·20 (+아이콘 18·글리프 15). 10.5·11.5·12.5·14.5 제거. 가짜 `--mono`/`--serif` 사용도 제거하고 숫자는 `font-variant-numeric:tabular-nums`로.
- **⑥ 좌우 패널 구분** — 왼쪽은 흰 목록, 오른쪽 뷰어는 회색 지면 + **어두운 제목 띠**(gray-80)로 "원문을 여는 창"임을 드러냄. 띠 위 버튼은 투명/흰색으로 반전.
- **⑦ 유령 노드** — 135° 대각선 빗금(공사장 테이프처럼 읽힘) 제거, 점선 테두리 + 평평한 옅은 바탕으로.
- **⑧ 헤더 지표** — 다섯 칸이 모두 같은 무게라 초점이 없던 것을, 핵심 둘(**연계 문서 186**, **열람 가능한 원문 0**)만 24px로 키우고 나머지는 17px 유지.

**되돌린 것**: 테두리를 너무 걷어 `.line .s`(결재선 조각) 바탕과 `.r-co` 역할 칩 바탕이 같은 회색이 되어 칩이 사라졌다(대비 1.0). 흰 바탕+테두리로 복구. `.cnt`도 옅은 카드 위에서 묻혀 테두리 복구.

**대비 보정**: `--amber`를 warning-50→60(4.23→5.29), `--warn`을 danger-50→60(4.07→5.31), 옅은 파랑 카드 위 보조문구를 `--ink-3`→`--ink-2`(4.01→7.73).

**공통 토큰 수정(다섯 페이지 전부 영향)**: `--ink-3`를 `gray-50`→`gray-60`. gray-50은 흰 바탕에서 4.13:1이라 본문 크기 보조 글자에 쓸 수 없었다. 흰 바탕 4.13→6.30, paper 4.13→5.77, panel-2 3.67→5.13, primary-5 4.01→5.61.

**SVG까지**: 스타일 블록만 고쳐 놓고 본문 인라인 SVG를 빠뜨렸다가 배포본에서 옛 빨강 5곳이 잡혔다. 그림 안 색 47곳도 KRDS 원시색으로 치환 (`#B8312F→bd2c0f`, `#7C8A9C→58616a`, `#D5DCE4→cdd1d5`, `#4B5A6E→464c53`, `#1F4E9C→083891`, `#1B2A41→33363d`, `#EEF1F4→f4f5f6`, `#a9dab4→d8eedd` 등). 검증 스크립트로 페이지 전체(HTML+CSS+SVG) KRDS 밖 색 0 확인.

**검증**: 페이지 전체 요소 444개를 훑어 WCAG AA 미달 0건(숨겨진 건너뛰기 링크 1건은 오탐). 계보 카드를 모두 펼친 상태에서도 0건. SVG 7개의 글자 대비도 미달 0건.

**이어서 볼 것**: 개요(index)는 `--cream/--dark/--acc/--ink0~2/--card/--line-c`라는 **자체 팔레트**를 쓴다(9/16 CryptoCalc 템플릿 교체분). 대비 미달 49건. 대조기 23건. 비교·연혁은 "근거·방법" 링크 1~2건. 2.0에 한 처방을 그대로 개요·대조기에 적용하면 다섯 페이지가 한 벌이 된다.

## 9/16 — 원문 결재란 실명 노출 수정
`docs/assets/jumin/p1.png` 결재란에 공무원 성명 4건이 그대로 공개돼 있었다. 붉은 가림 상자가 시행란만 덮고 결재란은 비껴갔다. 페이지 캡션("원문 결재란은 가렸다")·푸터("실명 제거")와 실제가 어긋난 상태였다. 성명 칸만 ○○○로 덮고 직위 머리줄과 전결 일자는 남겼다. p2~p7은 전수 확인 결과 실명 없음.
**남은 위험**: 가리기 전 판이 커밋 이력에 남아 있다. 지우려면 force-push가 필요한데 [[ops_github_suspension_2026-07]] 재발방지 수칙과 충돌해 하지 않았다. 문서 자체는 open.go.kr에 공개된 원문이라 새로 드러난 정보는 아니다.

## 9/16 — 다섯 페이지를 KRDS 한 벌로 (2.0 처방의 확장)
2.0에 한 처방을 나머지 네 페이지에 그대로 적용했다. 개요가 자체 팔레트를 쓴다고 봤는데, 열어 보니 `--cream/--ink0~2/--card` 등은 대부분 **KRDS 별칭**이었고 진짜 문제는 보조 글자색과 하드코딩 몇 개였다.

- **링크색**: `a{color}`를 primary-50 → **primary-60**. primary-50(#256ef4)은 흰 바탕 4.17:1이라 본문 링크에 쓸 수 없다. 네 페이지의 "근거·방법" 링크가 여기서 걸렸다. hover는 primary-70으로.
- **제도 색의 글자용 변형 신설**: `--rn/--pv/--og`는 점·테두리용 식별색인데 글자에도 쓰여 흰 바탕에서 4.16~4.26이 나왔다(대조기 `.st.bad`·`.st.mis`). `--rn-t(point-60)·--pv-t(warning-60)·--og-t(success-60)`를 만들고 **글자색으로 쓰인 22곳만** 치환. 테두리·배경 용도는 그대로 뒀다.
- **개요**: `--ink2` gray-50 → **gray-60**, `--card:#FFFFFF` → `var(--k-gray-0)`, `--r:10px` → `var(--r-l)`. 하드코딩 8종 치환 — 크림 틴트 `#FBF9F5`→gray-5·`#FCFBF8`→secondary-5(표 hover를 다른 페이지 선택색과 맞춤), 다크 절의 `#A7ADB8`→gray-30·`#C9CDD5`→gray-20·`#2F333B`→gray-80·`#F0A3A2`→point-10·`#8FD3D6`→success-10, 눈썹 점 `#2E6F73`→success-60.
- **비교**: 매트릭스 라벨색 `#6B5416`→warning-60, `#215659`→success-60. 본문 SVG 3종도 치환.
- **대조기·연혁**: CSS·본문 모두 이미 깨끗했고, 글자용 제도색 치환만 적용.

**검증**: 다섯 페이지를 iframe으로 띄워 전 요소 대비 검사 — **미달 0건**(이전 개요 49·대조기 23·비교 1·연혁 2). 빌드본 HTML+CSS+SVG를 통틀어 **KRDS 밖 색 0**.

**남은 것**: 430px에서 개요 병원 비유 SVG 글자가 실효 7px. 스택 전환이 필요하다.

## 9/16 — 법령 연혁: 연표 아래를 눌러야 보이던 카드 → 12건 전부 펼침
연표(세 줄·점 12개)는 그대로 두고, 그 아래 "점을 눌러야 열리는 카드 한 장"을 **시행일 순으로 합친 전체 목록**으로 바꿨다. 세 제도를 한 줄로 세우니 번갈아 손질된 순서가 그대로 드러난다 — 1998 정책실명제 → 2004 사전정보공표 → 2006 → 2011 → 2013 → **2014-02 정책실명제 부활 → 2014-03 원문공개 시행**(2주 차이) → 2014-11 → 2017 → 2018-06 → 2018-11 → 2020.

- **낱말 단위 대조 추가**(`tools/build-history-data.mjs`의 `wordDiff`): 정책실명제 §63~63의5 전문 1,334자가 다섯 번 거의 그대로 되풀이돼 읽을 수 없었다. 신설·조문 이동일 때만 전문을 싣고, 본문 개정은 **바뀐 자리만** 싣는다. 그 결과가 곧 서술이 됐다 — `안전행정부장관 → 행정자치부장관`('14.11), `행정자치부장관 → 행정안전부장관`('17), `조례 또는 규칙으로 → 조례로`('18.6), `직급 → 직급 또는 직위`('06), '18.11엔 국민신청 호가 통째로 신설. 데이터 29KB → 24KB.
- 항목 구성: 왼쪽 날짜 열(시행일·공포일·공포번호) + 제도 색 점 + 오른쪽 본문(제도명·개정구분·변화종류·법령명 → 조문 이름 → 개정이유 → 조문 전문 또는 바뀐 자리).
- 연표의 점은 이제 **해당 항목으로 스크롤**하고 잠깐 강조한다(누르지 않아도 내용은 이미 다 보인다).
- 다듬은 것: 조문 본문이 제목("제63조(정책의 실명 관리)")을 되풀이해 앞머리를 정규식으로 제거. 조문 이름이 괄호 안에서 줄이 나뉘어 이름마다 `inline-block`으로 감싸 이름 단위로만 끊기게.
- 검증: 항목 12개·순서 정상, 전문 5건·바뀐자리 7건, 가로 넘침 0, 대비 미달 0.

## 9/16 — A4 인쇄용 설명자료 (저장소 밖)
사이트 화면을 갈무리해 붙이고 아래에 주석과 설명을 다는 **A4 세로 14쪽** 자료.

- **저장소에 넣지 않는다.** 이 저장소는 public이고 `docs/`는 Pages로 게시되므로, 설명자료는 `brief/`에 두고 `.gitignore`로 제외했다. 도구만 저장소에 남긴다. 결과물은 `brief/정보공개-3개제도-실태조사-설명자료.pdf`(3.5MB).
- **구성**: ①표지 겸 요약(큰 숫자 4 + 다섯 화면 썸네일) ②추진 배경·산출물 ③개요 ④⑤제도 비교 1·2단 ⑥⑦⑧법령 연혁(연표·제개정이유·2014년 동시 정비) ⑨⑩사업 대조기(3채널·기관별) ⑪⑫정책실명제 2.0(통합 화면·연계 방식) ⑬수집 방법과 한계 ⑭조사 결과와 시사점.
- **문체**: 1차 원고가 구어체·수사가 많아 행정 문서 문체로 다시 썼다. 절 제목은 명사형(추진 배경 / 산출물 / 자료 수집 방법 및 한계 / 조사 결과 및 시사점), 본문은 행정 용어로 교체(실측→실태조사·등록자료, 읽히지 않는다→이용 실적 저조, 끊겨 있다→제도 간 연계 단절, 비어 있다→필수 항목 누락, 다음으로 준비할 것→후속 조치(안)).
- **주석 좌표를 손으로 찍지 않는다**: `tools/capture-shots.mjs`가 갈무리하면서 주석 대상의 위치를 **크롭 기준 백분율**로 뽑아 `brief/shots/marks.json`에 담고, `tools/build-brief.mjs`가 `/*__MARKS__*/` 자리에 주입한다. 화면이 바뀌면 다시 돌리기만 하면 번호가 따라간다. 번호는 대상의 왼쪽 위 모서리에 걸치게 둬 글자를 덜 덮는다.
- **인쇄 제약**: A4 여백 18mm면 본문 폭 174mm. 1440px 화면을 통째로 넣으면 14px 글자가 6px이 돼 읽을 수 없다. 그래서 **뷰포트 1180~1320px + 2배 해상도**로 찍고 화면마다 핵심 영역만 크롭했다. 쪽을 넘치는 그림은 `maxH`로 잘랐다.
- 만드는 순서: `capture-shots.mjs` → `build-brief.mjs` → `build-brief-pdf.mjs`. 마지막 도구가 쪽수·주석 수·깨진 이미지·넘친 쪽을 점검한 뒤 PDF로 뽑는다.

### 함정 메모 (추가)
- `maxH`는 **CSS 픽셀** 기준이다. `deviceScaleFactor:2`면 결과 PNG 높이는 그 두 배가 된다. 처음에 mm로 환산한 값을 그대로 넣어 두 배로 나왔다.
- `fullPage:true`로 구간을 잘라 찍으면 **`position:sticky` 요소가 그림 한가운데로 끼어든다**(상단 내비 바가 계보 그림 복판에 찍혔다). 갈무리 전에 sticky를 전부 static으로 바꿀 것.
- 인쇄 쪽번호를 `position:fixed`로 두면 **모든 쪽에 첫 쪽 번호가 겹쳐 찍힌다**. `@page{margin:0}` + 쪽 요소에 안쪽 여백을 주고 `position:absolute`로 앉힐 것.
- 공개 저장소에서는 `docs/`에 넣은 순간 게시된다. 보고용 문서는 만들기 전에 둘 자리를 먼저 정할 것. 이번엔 `docs/`에 만들었다가 커밋까지 한 뒤 옮겼고, 푸시 전이라 이력에는 남지 않았다.

## 9/16 — 비유 교체: 병원 창구 → 건설공사 실명판
처방전 비유가 어색하다는 지적. 처방전은 **지시서**이지 결정 기록이 아니어서 원문공개와 맞지 않았고, "담당의 명단"은 정책실명제의 핵심인 **사업 단위**를 담지 못했다.

- **새 대응**: 안내 표지판 = 사전정보공표 / 도면·서류 창고 = 원문공개 / **현장 실명판 = 정책실명제** / 건축물대장 번호 = 정책 번호.
- **고른 이유**: 건설공사 참여자 실명제는 1990년대 부실공사 대응으로 실제 도입돼 지금도 현장에 실명판이 붙는다. **비유가 아니라 같은 계열의 선례**다. "건설공사에는 이미 참여자 실명제가 있어 설계·시공·감리 책임자가 현장 실명판에 적힙니다. 정책에는 그 판이 없습니다."가 설명 없이 바로 통한다.
- 바꾼 곳: `docs/src/index.html` 비유 절(SVG 2장)과 세 제도 설명 카드, `docs/src/policy2.html` 리드·절 머리·SVG 2장, 설명자료 3쪽. `compare.html`의 "국민 신청 창구"는 제도 용어라 그대로 뒀다.
- **출처 없는 수치 제거**: 옛 SVG의 "처방전 3,800만 장"은 사이트 어디에도 각주가 없었다. 각주가 있는 "한 해 도면 143만 장"(2025년 원문공개 등록)으로 교체.
- 검증: 개요·2.0 두 쪽 대비 미달 0, SVG 글자 대비 미달 0, 옛 비유 낱말 0, 글자 겹침·잘림 0.

### 확인한 사실 (비유를 고르다 나온 것, 아직 사이트에 미반영)
결재문서에는 **이미 번호가 붙어 있다.** 수집한 원문공개 문서를 중복 제거해 세니 **840,297건 중 798,984건(95.1%)이 BRM 단위과제를 기재**한다(고유 과제 54,146종). 다만 그 번호는 **기능**을 가리켜 사업을 식별하지 못한다 — 정책실명제 고유 사업명 3,962건 중 단위과제명과 글자까지 같은 것은 **131건(3.3%)**, 한쪽이 다른 쪽을 포함하는 경우까지 넓혀도 **2,396건(60.5%)**. 이름이 겹치는 사업이 6할인데 기계가 이을 수 있는 건 3%다.
→ "결재 시 정책번호 선택"은 새 동작을 만드는 게 아니라 **이미 있는 선택 옆에 한 칸을 더하는 일**이라는 논거가 된다. 사이트·설명자료에 넣을지는 미정.

## 9/17 — 사용자 테스트(블라인드)와 수정
aside로 별도 에이전트에게 '정보공개 업무를 안 해본 중앙부처 사무관' 역할을 주고 과제 5개를 시켰다(첫인상 30초 / 제도 차이 찾기 / 대조기에서 사업 하나 확인 / 연혁에서 신설 연도와 이유 / 2.0이 현행인지 제안인지 판단). 평가가 아니라 **어디서 멈칫했는지**를 묻게 했다.

### 잡힌 버그 (전부 재현 확인 후 수정)
1. **비교 딥링크가 스크롤되지 않음** — 개요의 "실측 근거 ›" 9개가 `compare.html#fields`로 보내는데, 패널은 열리지만 화면은 맨 위 그대로였다(패널은 뷰포트 아래 1,604px). 대상 id가 문서에 없어 브라우저 기본 앵커 점프도 안 걸렸다. 이용자는 "링크가 안 먹었나" 하고 다시 눌렀다. → 딥링크로 들어오면 그 자리로 즉시 이동, 상호작용 시에는 부드럽게.
2. **해시만 바뀌면 패널이 안 따라옴**(뒤로/앞으로 포함) → `hashchange` 처리 추가.
3. **정책실명제 2.0의 버튼 6개 무반응** — 링크 복사·담기·JSON·PDF 열기·붙임 zip 받기·부산시 정책 페이지로. `‹ ›` 쪽 넘김은 정상이었다(속성이 아니라 프로퍼티로 연결돼 있어 처음 점검에서 오판). → **링크 복사는 실제로 구현**(clipboard 실패 시 execCommand 대체), 나머지는 `.btn.demo`로 점선·흐림 처리하고 `disabled`+"시안" 꼬리표.
4. **186 vs 184** — 상단 지표만 186이고 본문·별첨은 전부 184라 오류로 읽혔다. → 라벨을 "연계 문서 · '24 2 + '25 184"로 바꿔 셈이 보이게.
5. **연표 점을 눌러도 주소가 안 바뀜** — 특정 개정 항목을 링크로 보낼 수 없었다. → `#ev-<제도>-<시행일>` 앵커 부여 + 그 주소로 들어오면 해당 항목으로 이동·강조.

### 고친 이용성
- **대조기 한 줄 판정 신설** — "이 사업은 사전정보공표 있음 2건 · 원문공개 있음 1건". 세 칸을 다 읽고 이용자가 결론내야 했다는 지적. 둘 다 0이면 왼쪽 강조선과 "두 채널 모두 없어 정책실명제에만 있습니다".
- **아홉 기준이 전부인 줄 알았다**는 지적 → 1층 표 아래에 "아홉 기준은 열다섯 항목 중 핵심만 고른 것" 한 줄.
- **행 이름과 항목 이름이 달라 잘못 눌렀나 싶었다**("예산·집행 정보" → "공개 항목") → 항목명을 "공개 항목 · 예산·집행"으로.
- **연혁 별첨 문체**가 다른 페이지 별첨(간결체)과 달리 습니다체였다 → 14곳 통일. 본문 습니다체 / 별첨·각주 간결체 / 제목·판정 명사형이 이 사이트의 규칙이다.

### 내가 낸 실수
판정 줄을 넣으며 probe 필드를 `pr.orig`로 썼는데 실제 이름은 **`pr.orginl`**이었다. 게다가 방어 없이 `.err`를 읽어 `select()` 전체가 TypeError로 죽었고 **상세 화면이 통째로 안 그려졌다.** 콘솔 로그를 보고 잡았다. 교훈: 데이터 필드명은 빌드본에서 확인하고, 남의 데이터를 읽을 땐 존재 검사부터.

### 남은 넷도 처리 (9/17)
- **티커** — 오른쪽에 "마우스를 올리면 멈춥니다" 고정 안내, `:focus-within`에도 정지, `prefers-reduced-motion`이면 아예 멈추고 안내를 "실측치"로 바꿈.
- **범례** — `.legend:before`로 "범례" 꼬리표를 달고 `cursor:default`. 색 칩을 필터로 오인해 누르는 일을 막는다. 다섯 쪽 공통.
- **2.0이 현행으로 오인됨** — 화면 바로 위에 시안 띠 신설. "아래 화면은 제안입니다. 수치·문서명·결재선 역할은 포털에서 받은 실측치이고, 정책번호와 점선 버튼 동작은 가상입니다." 눈썹·각주만으로는 어디까지가 시안인지 몰랐다는 지적을 그대로 반영.
- **0건 칸이 빈 상자** — `.cols`를 `align-items:start`로 바꿔 억지로 높이를 맞추지 않고, 0건이면 "왜 비어 있나"를 채운다(사전정보: 사업명이 아니라 정보 항목 단위라서 / 원문: 국장급 이상 결재분만 대상이라서).
- **덤** — 2.0에서 문서를 골라도 주소가 안 바뀌어 '링크 복사'가 늘 같은 주소를 복사했다. `#d=<문서>` 앵커를 부여하고 그 주소로 들어오면 해당 문서를 열도록. 확인: 선택→주소 추적 4/4, 딥링크 복원 정상, 해시 변경도 따라옴.

## 9/17 — 분석 노트 ① 국민이 신청한 정책실명제
제도 개선 인사이트를 뽑는 분석 게시판의 첫 편. 물음 하나를 여러 축에서 보고, 보고서에 그대로 옮길 문단을 붙인다.

- **구조**: 물음 → 한 줄 답 → 축별 근거(그림) → 그래서(개선 방향) → 보고서용 발췌(복사) → 방법과 한계.
- **축 셋**: 시간 × 법령 / 선정기준 × 이용 / 기관 × 선정기준.
- **산출 코드** `tools/analyze-citizen.mjs` → `notes/citizen.json` → 빌드가 `/*__NOTEDATA__*/`에 주입. 수치를 손으로 적지 않으므로 재수집하면 글과 그림이 따라 바뀐다.

### 실측
- 국민신청 선정 **135건 / 6,847건 = 2.0%**, 24개 기관.
- 연도별 **34 → 35 → 20 → 20 → 10 → 11 → 3 → 2 → 0**. 조문(제63조의3①6, 2018-11-27 시행)이 생긴 해가 정점이고 이후 내리막. 조문 이전에도 기관들은 이미 '국민신청'을 적고 있었다.
- 기관 편중: 교육부 33 · 국방부 25 · 국가유산청 11. 표기도 흔들림("국민신청" 115, "국민 신청" 2, 복합 표기 14).

### 방법에서 걸러낸 함정 — 이게 이 노트의 핵심
조회수 가운데값이 국민신청 5회 대 그 외 3회라 **1.7배처럼 보였다.** 그런데 포털 조회수는 **누적값**이고 국민신청은 2018~2019년에 몰려 있다. 표본 5건 이상인 해만 골라 **같은 해끼리** 견주니 **1.12배**였다. "국민이 고른 사업이 더 읽힌다"는 주장은 성립하지 않는다. 보정 없이 썼으면 틀린 문장이 보고서로 넘어갔을 것이다.

### 결론(개선 방향)
조문은 이미 있다. 없는 것은 **절차와 창구**다. 지금의 '국민신청'은 국민이 신청한 기록이 아니라 **기관의 자기 기재**다. 2026년 0건은 국민이 관심을 잃은 결과가 아니라 신청할 곳이 없던 결과로 읽는 편이 사실에 가깝다. 효과 주장은 창구를 열고 표본이 쌓여야 가능하다(2025~26 상세 784건 중 국민신청은 2건뿐이라 추진실적·원문연결 비교 불가).

### 함정 메모 (추가)
- 막대그림에 세로 기준선을 그을 때 **막대 사이 빈 자리**에 세워야 한다. 막대 i가 `L+i*bw+0.22bw`에서 시작해 `0.56bw` 폭이면 틈 한가운데는 `L+(i+1)*bw`. 처음에 `L+1.42bw`로 잡아 2019년 막대와 값 글자를 관통했다.
- aside가 **캐시된 판을 보고 "아직 관통한다"고 보고**했다. 좌표를 직접 재서 확인할 것(점선 418px, 막대 437~487px).

## 9/17 — 조회수 해석 정정과 분석 게시판 카테고리 정비
**사용자 지적**: 정보공개포털 조회수는 실제 열람을 재지 못한다. 사람들은 각 기관 누리집에서 본다.

**조문으로 확인했다.** 「행정업무의 운영 및 혁신에 관한 규정」 **제63조의3제3항** — "행정기관의 장은 정책실명제 중점관리 대상사업의 추진실적을 **해당 기관의 인터넷 홈페이지 등**을 통하여 공개하여야 한다." **정보공개포털은 조문에 없다.** 포털은 모아 보여 주는 화면이고, 법이 정한 공개 장소는 기관 누리집이다.

그동안 "조회 가운데값 1회", "한 번도 열리지 않은 사업 68%"를 제도의 도달 지표처럼 써 왔다. 절반만 맞는 말이었다. 다섯 쪽에서 고쳤다.
- 비교 2층 '이용' 판정: "정책실명제는 조회가 거의 없음" → "포털에서는 거의 열리지 않는다. 다만 이 수치로 제도의 도달을 재기는 어렵다"
- **각주 [5]를 뒤집었다.** 전에는 "낮은 쪽으로 편향되지는 않는다"고 적었는데 틀렸다. 봇·내부확인으로 **부풀 수도**, 기관 누리집 열람이 빠져 **줄어들 수도** 있다. 양방향 오차다.
- 개요 문구, 대조기 열 설명·한계, 각주 정본(n_rn_inq) 모두 "정보공개포털에서 열린 횟수"로 한정.

**분석 게시판을 다섯 갈래로 정비했다.** 갈래마다 지금까지 드러난 것을 한 줄로 적고, 노트가 있으면 걸고 없으면 '준비 중'으로 둔다. 빈 칸이 다음에 쓸 글이다.
① **신청제** — 신청 제도는 생겼으나 활성화되지 않았다 (노트 ① 게시)
② **공개 장소** — 공개해야 할 곳은 기관 누리집인데 재는 곳은 포털이다
③ **연계** — 같은 사업이 세 채널에서 이어지지 않는다
④ **책임 기재** — 결재선 밖을 적으려던 제도가 결재선을 옮겨 적는다
⑤ **법령 계보** — 조문은 고쳤으나 운영이 따라오지 않았다

**노트 ①의 축 2를 다시 썼다.** "더 읽힌다는 인상은 사라진다"에서 **"효과를 재려 했으나 지금 자료로는 잴 수 없다"**로. 막는 것이 둘이다. 누적 편향(1.7배→1.12배)과 측정 장소(조문은 기관 누리집). 결론은 "효과가 없다"가 아니라 "잴 수 없다"이며, 창구를 열고 기관 누리집 접속 자료를 함께 봐야 판단할 수 있다.

## 9/17 — "창구가 없다"는 틀렸다 (사용자 확인 요구 → 실사)
사용자가 "각 부처가 신청 게시판이 있긴 있다"고 되물었다. 노트 ①에 **"신청할 창구를 만들지 않았다"**고 단정해 게시한 상태였다. 확인해 보니 **내 주장이 과했다.**

**법령정보센터 확인** — 규정 제63조의5①이 "행정안전부장관이 정한다"고 위임했는데, 정책실명제를 다루는 행정규칙은 **관세청 훈령(2015)과 교육부 운영규정(2023) 둘뿐이고 행정안전부 소관은 없다.** 다만 행안부 누리집이 "운영지침 통보(행안부)"를 절차 첫 단계로 적고 있어, 훈령·예규로 공개되지 않았을 뿐 지침 자체는 있을 수 있다.
교육부 훈령 제5조①이 눈에 띈다 — 정책실명제심의위원회를 **「교육부 국민 및 공무원 제안 운영규정」의 제안심사위원회와 통합 운영할 수 있다.** 별도 창구가 아니라 기존 국민제안 체계에 얹는 구조다.

**누리집 세 곳 실사(aside)**
- **정보공개포털**: 정책실명제 코너는 조회 전용. 포털의 '신청' 메뉴는 정보공개청구·이의신청뿐. **접수 기능 없음.**
- **행정안전부**: 전용 안내 페이지 있음. 선정절차 도식에 "(국민) 사업 신청 — 문서24, 온국민소통, 이메일, 방문 우편"이라 적고 **신청서 한글 파일을 내려받게** 한다. 화면에서 바로 내는 자리는 없다.
- **교육부**: 「국민신청실명제」 **상설 메뉴에 온라인 신청 서식이 실제로 있다.** 성명·사업명·신청사유 입력, 신청자격 모든 국민, 기간 수시.

**정정한 것** — "창구가 없다" → **"통합 창구가 없고 부처마다 제각각이다."** 포털에 접수 기능이 없고 주무부처도 서식 배포에 그치는 반면 교육부에는 온라인 폼이 있다. 그리고 **국민신청 선정 1위가 교육부 33건**이다. "창구가 있는 곳에서 신청이 나온다"는 가설을 세울 만하지만 세 기관만 봤으므로 단정하지 않는다. 각주 [7][8]에 확인 범위와 지침 미확인을 명시했다.

**교훈**: 없다는 주장은 있다는 주장보다 확인 비용이 크다. "포털에 없다"까지만 확인하고 "창구가 없다"로 일반화했다. 부처 누리집을 안 본 채로 쓴 문장이었다.
